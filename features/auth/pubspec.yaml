name: auth
description: 登入功能:domain/data 層、AuthTokenRefreshGateway、路由與 DI 註冊。
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  bloc: ^9.0.0
  core: any
  flutter:
    sdk: flutter
  flutter_bloc: ^9.0.0
  get_it: ^9.0.0
  go_router: ^18.0.0
  localization: any
  ui: any

dev_dependencies:
  bloc_test: ^10.0.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
